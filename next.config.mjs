const config = {
  // Remove or comment out the i18n configuration
  // i18n: {
  //   locales: ['en'],
  //   defaultLocale: 'en',
  // },
  output: 'export',
  images: {
    unoptimized: true,
  },
};

module.exports = config;
